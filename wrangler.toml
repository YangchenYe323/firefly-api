name = "firefly-api"
main = "build/worker/shim.mjs"
compatibility_date = "2025-06-22"

[build]
command = "cargo install -q worker-build && worker-build --release"

[[r2_buckets]]
bucket_name = "firefly-bilibili"
binding = "firefly_bilibili"